import { p } from "./helpers";

export const twoPointerProblems = [
  p({
    id: "valid-palindrome",
    title: "Valid Palindrome",
    difficulty: "easy",
    topicId: "two-pointers",
    leetcodeId: 125,
    companies: ["Meta", "Microsoft", "Apple"],
    description: "A phrase is a palindrome if, after converting to lowercase and removing non-alphanumeric characters, it reads the same forward and backward.",
    examples: [
      { input: 's = "A man, a plan, a canal: Panama"', output: "true" },
      { input: 's = "race a car"', output: "false" },
    ],
    constraints: ["1 <= s.length <= 2 * 10^5"],
    hints: ["Use two pointers from both ends.", "Skip non-alphanumeric characters."],
    approach: "Two pointers from left and right, skip non-alphanumeric, compare lowercase chars.",
    timeComplexity: "O(n)",
    spaceComplexity: "O(1)",
    tags: ["two-pointers", "string"],
    starterCode: {
      javascript: 'function isPalindrome(s) {\n  // Your code here\n}',
      python: "def isPalindrome(s):\n    pass",
      java: "class Solution {\n    public boolean isPalindrome(String s) {\n    }\n}",
    },
    solution: {
      javascript: 'function isPalindrome(s) {\n  let l = 0, r = s.length - 1;\n  while (l < r) {\n    while (l < r && !/[a-z0-9]/i.test(s[l])) l++;\n    while (l < r && !/[a-z0-9]/i.test(s[r])) r--;\n    if (s[l].toLowerCase() !== s[r].toLowerCase()) return false;\n    l++; r--;\n  }\n  return true;\n}',
      python: "def isPalindrome(s):\n    s = ''.join(c.lower() for c in s if c.isalnum())\n    return s == s[::-1]",
      java: "class Solution {\n    public boolean isPalindrome(String s) {\n        int l = 0, r = s.length()-1;\n        while (l < r) {\n            while (l < r && !Character.isLetterOrDigit(s.charAt(l))) l++;\n            while (l < r && !Character.isLetterOrDigit(s.charAt(r))) r--;\n            if (Character.toLowerCase(s.charAt(l)) != Character.toLowerCase(s.charAt(r))) return false;\n            l++; r--;\n        }\n        return true;\n    }\n}",
    },
  }),
  p({
    id: "two-sum-ii",
    title: "Two Sum II - Input Array Is Sorted",
    difficulty: "medium",
    topicId: "two-pointers",
    patternId: "two-sum-pattern",
    leetcodeId: 167,
    companies: ["Amazon", "Apple", "Google"],
    description: "Given a 1-indexed sorted array, find two numbers that add up to `target`. Return indices (1-indexed).",
    examples: [
      { input: "numbers = [2,7,11,15], target = 9", output: "[1,2]" },
    ],
    constraints: ["2 <= numbers.length <= 3 * 10^4", "Array is sorted in non-decreasing order"],
    hints: ["Use two pointers at start and end.", "If sum is too small, move left pointer right."],
    approach: "Two pointers at both ends. If sum < target, increment left; if sum > target, decrement right.",
    timeComplexity: "O(n)",
    spaceComplexity: "O(1)",
    tags: ["array", "two-pointers"],
    starterCode: {
      javascript: "function twoSum(numbers, target) {\n  // Your code here\n}",
      python: "def twoSum(numbers, target):\n    pass",
      java: "class Solution {\n    public int[] twoSum(int[] numbers, int target) {\n    }\n}",
    },
    solution: {
      javascript: "function twoSum(numbers, target) {\n  let l = 0, r = numbers.length - 1;\n  while (l < r) {\n    const sum = numbers[l] + numbers[r];\n    if (sum === target) return [l+1, r+1];\n    if (sum < target) l++; else r--;\n  }\n  return [];\n}",
      python: "def twoSum(numbers, target):\n    l, r = 0, len(numbers)-1\n    while l < r:\n        s = numbers[l]+numbers[r]\n        if s == target: return [l+1,r+1]\n        l += s < target; r -= s > target\n    return []",
      java: "class Solution {\n    public int[] twoSum(int[] numbers, int target) {\n        int l = 0, r = numbers.length-1;\n        while (l < r) {\n            int sum = numbers[l]+numbers[r];\n            if (sum == target) return new int[]{l+1,r+1};\n            if (sum < target) l++; else r--;\n        }\n        return new int[]{};\n    }\n}",
    },
  }),
  p({
    id: "3sum",
    title: "3Sum",
    difficulty: "medium",
    topicId: "two-pointers",
    patternId: "two-sum-pattern",
    leetcodeId: 15,
    companies: ["Meta", "Amazon", "Google", "Microsoft"],
    description: "Given an integer array `nums`, return all unique triplets `[nums[i], nums[j], nums[k]]` such that they sum to zero.",
    examples: [
      { input: "nums = [-1,0,1,2,-1,-4]", output: "[[-1,-1,2],[-1,0,1]]" },
    ],
    constraints: ["3 <= nums.length <= 3000", "-10^5 <= nums[i] <= 10^5"],
    hints: ["Sort the array first.", "Fix one element and use two pointers for the remaining two.", "Skip duplicates."],
    approach: "Sort array. For each i, use two pointers on subarray to find pairs summing to -nums[i]. Skip duplicates at all levels.",
    timeComplexity: "O(n²)",
    spaceComplexity: "O(1) excluding output",
    tags: ["array", "two-pointers", "sorting"],
    starterCode: {
      javascript: "function threeSum(nums) {\n  // Your code here\n}",
      python: "def threeSum(nums):\n    pass",
      java: "class Solution {\n    public List<List<Integer>> threeSum(int[] nums) {\n    }\n}",
    },
    solution: {
      javascript: "function threeSum(nums) {\n  nums.sort((a,b)=>a-b); const res = [];\n  for (let i = 0; i < nums.length-2; i++) {\n    if (i > 0 && nums[i] === nums[i-1]) continue;\n    let l = i+1, r = nums.length-1;\n    while (l < r) {\n      const sum = nums[i]+nums[l]+nums[r];\n      if (sum === 0) { res.push([nums[i],nums[l],nums[r]]); l++; r--; while(l<r&&nums[l]===nums[l-1])l++; while(l<r&&nums[r]===nums[r+1])r--; }\n      else if (sum < 0) l++; else r--;\n    }\n  }\n  return res;\n}",
      python: "def threeSum(nums):\n    nums.sort(); res = []\n    for i in range(len(nums)-2):\n        if i and nums[i]==nums[i-1]: continue\n        l,r=i+1,len(nums)-1\n        while l<r:\n            s=nums[i]+nums[l]+nums[r]\n            if s==0: res.append([nums[i],nums[l],nums[r]]); l+=1; r-=1\n            elif s<0: l+=1\n            else: r-=1\n    return res",
      java: "class Solution {\n    public List<List<Integer>> threeSum(int[] nums) {\n        Arrays.sort(nums); List<List<Integer>> res = new ArrayList<>();\n        for (int i = 0; i < nums.length-2; i++) {\n            if (i > 0 && nums[i] == nums[i-1]) continue;\n            int l = i+1, r = nums.length-1;\n            while (l < r) {\n                int sum = nums[i]+nums[l]+nums[r];\n                if (sum == 0) { res.add(Arrays.asList(nums[i],nums[l],nums[r])); l++; r--; while(l<r&&nums[l]==nums[l-1])l++; while(l<r&&nums[r]==nums[r+1])r--; }\n                else if (sum < 0) l++; else r--;\n            }\n        }\n        return res;\n    }\n}",
    },
  }),
  p({
    id: "container-most-water",
    title: "Container With Most Water",
    difficulty: "medium",
    topicId: "two-pointers",
    leetcodeId: 11,
    companies: ["Google", "Amazon", "Meta"],
    description: "Given `n` non-negative integers representing heights, find two lines that together with the x-axis form a container that holds the most water.",
    examples: [
      { input: "height = [1,8,6,2,5,4,8,3,7]", output: "49" },
    ],
    constraints: ["n == height.length", "2 <= n <= 10^5"],
    hints: ["Two pointers at both ends.", "Move the pointer with the shorter height inward."],
    approach: "Two pointers at ends. Area = min(h[l],h[r]) * (r-l). Move the shorter side inward.",
    timeComplexity: "O(n)",
    spaceComplexity: "O(1)",
    tags: ["array", "two-pointers"],
    starterCode: {
      javascript: "function maxArea(height) {\n  // Your code here\n}",
      python: "def maxArea(height):\n    pass",
      java: "class Solution {\n    public int maxArea(int[] height) {\n    }\n}",
    },
    solution: {
      javascript: "function maxArea(height) {\n  let l = 0, r = height.length-1, max = 0;\n  while (l < r) {\n    max = Math.max(max, Math.min(height[l], height[r]) * (r-l));\n    if (height[l] < height[r]) l++; else r--;\n  }\n  return max;\n}",
      python: "def maxArea(height):\n    l,r,ans=0,len(height)-1,0\n    while l<r:\n        ans=max(ans,min(height[l],height[r])*(r-l))\n        l+=height[l]<height[r]; r-=height[l]>=height[r]\n    return ans",
      java: "class Solution {\n    public int maxArea(int[] height) {\n        int l=0,r=height.length-1,max=0;\n        while(l<r){ max=Math.max(max,Math.min(height[l],height[r])*(r-l)); if(height[l]<height[r])l++;else r--; }\n        return max;\n    }\n}",
    },
  }),
  p({
    id: "trapping-rain-water",
    title: "Trapping Rain Water",
    difficulty: "hard",
    topicId: "two-pointers",
    leetcodeId: 42,
    companies: ["Amazon", "Google", "Goldman Sachs"],
    description: "Given `n` non-negative integers representing an elevation map, compute how much water it can trap after raining.",
    examples: [
      { input: "height = [0,1,0,2,1,0,1,3,2,1,2,1]", output: "6" },
    ],
    constraints: ["n == height.length", "1 <= n <= 2 * 10^4"],
    hints: ["Water at i = min(maxLeft, maxRight) - height[i].", "Two-pointer approach tracks max from both sides."],
    approach: "Two pointers with leftMax and rightMax. Process the side with smaller max height.",
    timeComplexity: "O(n)",
    spaceComplexity: "O(1)",
    tags: ["array", "two-pointers"],
    starterCode: {
      javascript: "function trap(height) {\n  // Your code here\n}",
      python: "def trap(height):\n    pass",
      java: "class Solution {\n    public int trap(int[] height) {\n    }\n}",
    },
    solution: {
      javascript: "function trap(height) {\n  let l=0,r=height.length-1,lMax=0,rMax=0,res=0;\n  while(l<r){\n    if(height[l]<height[r]){ lMax=Math.max(lMax,height[l]); res+=lMax-height[l]; l++; }\n    else { rMax=Math.max(rMax,height[r]); res+=rMax-height[r]; r--; }\n  }\n  return res;\n}",
      python: "def trap(height):\n    l,r,lmax,rmax,res=0,len(height)-1,0,0,0\n    while l<r:\n        if height[l]<height[r]: lmax=max(lmax,height[l]); res+=lmax-height[l]; l+=1\n        else: rmax=max(rmax,height[r]); res+=rmax-height[r]; r-=1\n    return res",
      java: "class Solution {\n    public int trap(int[] height) {\n        int l=0,r=height.length-1,lMax=0,rMax=0,res=0;\n        while(l<r){ if(height[l]<height[r]){ lMax=Math.max(lMax,height[l]); res+=lMax-height[l]; l++; } else { rMax=Math.max(rMax,height[r]); res+=rMax-height[r]; r--; } }\n        return res;\n    }\n}",
    },
  }),
];
