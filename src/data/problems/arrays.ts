import { p } from "./helpers";

export const arrayProblems = [
  p({
    id: "two-sum",
    title: "Two Sum",
    difficulty: "easy",
    topicId: "arrays-hashing",
    patternId: "two-sum-pattern",
    leetcodeId: 1,
    companies: ["Google", "Amazon", "Meta", "Microsoft", "Apple"],
    description:
      "Given an array of integers `nums` and an integer `target`, return indices of the two numbers such that they add up to `target`. You may assume each input has exactly one solution.",
    examples: [
      { input: "nums = [2,7,11,15], target = 9", output: "[0,1]", explanation: "nums[0] + nums[1] == 9" },
      { input: "nums = [3,2,4], target = 6", output: "[1,2]" },
    ],
    constraints: ["2 <= nums.length <= 10^4", "-10^9 <= nums[i] <= 10^9", "Exactly one valid answer exists"],
    hints: [
      "A brute force approach checks every pair — O(n²). Can you do better?",
      "Use a hash map to store values you've seen and their indices.",
      "For each number, check if (target - number) exists in the map.",
    ],
    approach:
      "Use a hash map to store each number and its index. For each element, check if its complement (target - nums[i]) exists in the map. If yes, return both indices. Otherwise, add the current number to the map.",
    timeComplexity: "O(n)",
    spaceComplexity: "O(n)",
    tags: ["array", "hash-table"],
    starterCode: {
      javascript: "function twoSum(nums, target) {\n  // Your code here\n}",
      python: "def twoSum(nums, target):\n    # Your code here\n    pass",
      java: "class Solution {\n    public int[] twoSum(int[] nums, int target) {\n        // Your code here\n    }\n}",
    },
    solution: {
      javascript: "function twoSum(nums, target) {\n  const map = new Map();\n  for (let i = 0; i < nums.length; i++) {\n    const complement = target - nums[i];\n    if (map.has(complement)) return [map.get(complement), i];\n    map.set(nums[i], i);\n  }\n  return [];\n}",
      python: "def twoSum(nums, target):\n    seen = {}\n    for i, num in enumerate(nums):\n        if target - num in seen:\n            return [seen[target - num], i]\n        seen[num] = i",
      java: "class Solution {\n    public int[] twoSum(int[] nums, int target) {\n        Map<Integer, Integer> map = new HashMap<>();\n        for (int i = 0; i < nums.length; i++) {\n            int complement = target - nums[i];\n            if (map.containsKey(complement)) return new int[]{map.get(complement), i};\n            map.put(nums[i], i);\n        }\n        return new int[]{};\n    }\n}",
    },
  }),
  p({
    id: "contains-duplicate",
    title: "Contains Duplicate",
    difficulty: "easy",
    topicId: "arrays-hashing",
    leetcodeId: 217,
    companies: ["Amazon", "Apple", "Google"],
    description: "Given an integer array `nums`, return `true` if any value appears at least twice, and `false` if every element is distinct.",
    examples: [
      { input: "nums = [1,2,3,1]", output: "true" },
      { input: "nums = [1,2,3,4]", output: "false" },
    ],
    constraints: ["1 <= nums.length <= 10^5", "-10^9 <= nums[i] <= 10^9"],
    hints: ["Use a Set to track seen elements.", "Alternatively, sort and check adjacent elements."],
    approach: "Insert each element into a Set. If an element already exists, return true.",
    timeComplexity: "O(n)",
    spaceComplexity: "O(n)",
    tags: ["array", "hash-table"],
    starterCode: {
      javascript: "function containsDuplicate(nums) {\n  // Your code here\n}",
      python: "def containsDuplicate(nums):\n    pass",
      java: "class Solution {\n    public boolean containsDuplicate(int[] nums) {\n    }\n}",
    },
    solution: {
      javascript: "function containsDuplicate(nums) {\n  return new Set(nums).size !== nums.length;\n}",
      python: "def containsDuplicate(nums):\n    return len(set(nums)) != len(nums)",
      java: "class Solution {\n    public boolean containsDuplicate(int[] nums) {\n        Set<Integer> set = new HashSet<>();\n        for (int n : nums) if (!set.add(n)) return true;\n        return false;\n    }\n}",
    },
  }),
  p({
    id: "valid-anagram",
    title: "Valid Anagram",
    difficulty: "easy",
    topicId: "arrays-hashing",
    leetcodeId: 242,
    companies: ["Amazon", "Facebook", "Bloomberg"],
    description: "Given two strings `s` and `t`, return `true` if `t` is an anagram of `s`, and `false` otherwise.",
    examples: [
      { input: 's = "anagram", t = "nagaram"', output: "true" },
      { input: 's = "rat", t = "car"', output: "false" },
    ],
    constraints: ["1 <= s.length, t.length <= 5 * 10^4", "s and t consist of lowercase English letters"],
    hints: ["Count character frequencies in both strings.", "If lengths differ, they cannot be anagrams."],
    approach: "Count character frequencies in s, decrement for t. All counts should be zero.",
    timeComplexity: "O(n)",
    spaceComplexity: "O(1)",
    tags: ["string", "hash-table", "sorting"],
    starterCode: {
      javascript: 'function isAnagram(s, t) {\n  // Your code here\n}',
      python: "def isAnagram(s, t):\n    pass",
      java: "class Solution {\n    public boolean isAnagram(String s, String t) {\n    }\n}",
    },
    solution: {
      javascript: 'function isAnagram(s, t) {\n  if (s.length !== t.length) return false;\n  const count = {};\n  for (const c of s) count[c] = (count[c] || 0) + 1;\n  for (const c of t) { if (!count[c]) return false; count[c]--; }\n  return true;\n}',
      python: "def isAnagram(s, t):\n    return sorted(s) == sorted(t)",
      java: "class Solution {\n    public boolean isAnagram(String s, String t) {\n        if (s.length() != t.length()) return false;\n        int[] count = new int[26];\n        for (char c : s.toCharArray()) count[c-'a']++;\n        for (char c : t.toCharArray()) if (--count[c-'a'] < 0) return false;\n        return true;\n    }\n}",
    },
  }),
  p({
    id: "group-anagrams",
    title: "Group Anagrams",
    difficulty: "medium",
    topicId: "arrays-hashing",
    leetcodeId: 49,
    companies: ["Amazon", "Meta", "Google"],
    description: "Given an array of strings `strs`, group the anagrams together. You can return the answer in any order.",
    examples: [
      { input: 'strs = ["eat","tea","tan","ate","nat","bat"]', output: '[["bat"],["nat","tan"],["ate","eat","tea"]]' },
    ],
    constraints: ["1 <= strs.length <= 10^4", "0 <= strs[i].length <= 100"],
    hints: ["Use sorted string or character count as hash key.", "Group strings with the same key together."],
    approach: "Use a hash map with sorted string (or char count tuple) as key. Group strings sharing the same key.",
    timeComplexity: "O(n * k log k)",
    spaceComplexity: "O(n * k)",
    tags: ["array", "hash-table", "string"],
    starterCode: {
      javascript: "function groupAnagrams(strs) {\n  // Your code here\n}",
      python: "def groupAnagrams(strs):\n    pass",
      java: "class Solution {\n    public List<List<String>> groupAnagrams(String[] strs) {\n    }\n}",
    },
    solution: {
      javascript: "function groupAnagrams(strs) {\n  const map = new Map();\n  for (const s of strs) {\n    const key = s.split('').sort().join('');\n    if (!map.has(key)) map.set(key, []);\n    map.get(key).push(s);\n  }\n  return [...map.values()];\n}",
      python: "def groupAnagrams(strs):\n    from collections import defaultdict\n    groups = defaultdict(list)\n    for s in strs:\n        groups[tuple(sorted(s))].append(s)\n    return list(groups.values())",
      java: "class Solution {\n    public List<List<String>> groupAnagrams(String[] strs) {\n        Map<String, List<String>> map = new HashMap<>();\n        for (String s : strs) {\n            char[] arr = s.toCharArray(); Arrays.sort(arr);\n            String key = new String(arr);\n            map.computeIfAbsent(key, k -> new ArrayList<>()).add(s);\n        }\n        return new ArrayList<>(map.values());\n    }\n}",
    },
  }),
  p({
    id: "product-except-self",
    title: "Product of Array Except Self",
    difficulty: "medium",
    topicId: "arrays-hashing",
    leetcodeId: 238,
    companies: ["Amazon", "Meta", "Microsoft", "Apple"],
    description: "Given an integer array `nums`, return an array `answer` such that `answer[i]` is the product of all elements except `nums[i]`. Solve it in O(n) without using division.",
    examples: [
      { input: "nums = [1,2,3,4]", output: "[24,12,8,6]" },
    ],
    constraints: ["2 <= nums.length <= 10^5", "-30 <= nums[i] <= 30"],
    hints: ["Compute prefix products from left, then suffix from right.", "You can do this in O(1) extra space using output array."],
    approach: "Two passes: first compute prefix products into result array, then multiply by suffix products in a second pass.",
    timeComplexity: "O(n)",
    spaceComplexity: "O(1) excluding output",
    tags: ["array", "prefix-sum"],
    starterCode: {
      javascript: "function productExceptSelf(nums) {\n  // Your code here\n}",
      python: "def productExceptSelf(nums):\n    pass",
      java: "class Solution {\n    public int[] productExceptSelf(int[] nums) {\n    }\n}",
    },
    solution: {
      javascript: "function productExceptSelf(nums) {\n  const n = nums.length, res = new Array(n).fill(1);\n  let prefix = 1;\n  for (let i = 0; i < n; i++) { res[i] = prefix; prefix *= nums[i]; }\n  let suffix = 1;\n  for (let i = n - 1; i >= 0; i--) { res[i] *= suffix; suffix *= nums[i]; }\n  return res;\n}",
      python: "def productExceptSelf(nums):\n    n = len(nums); res = [1]*n; p = 1\n    for i in range(n): res[i] = p; p *= nums[i]\n    s = 1\n    for i in range(n-1,-1,-1): res[i] *= s; s *= nums[i]\n    return res",
      java: "class Solution {\n    public int[] productExceptSelf(int[] nums) {\n        int n = nums.length; int[] res = new int[n];\n        res[0] = 1;\n        for (int i = 1; i < n; i++) res[i] = res[i-1] * nums[i-1];\n        int suffix = 1;\n        for (int i = n-1; i >= 0; i--) { res[i] *= suffix; suffix *= nums[i]; }\n        return res;\n    }\n}",
    },
  }),
  p({
    id: "top-k-frequent",
    title: "Top K Frequent Elements",
    difficulty: "medium",
    topicId: "arrays-hashing",
    patternId: "top-k-heap",
    leetcodeId: 347,
    companies: ["Amazon", "Meta", "Google", "Uber"],
    description: "Given an integer array `nums` and an integer `k`, return the `k` most frequent elements. You may return the answer in any order.",
    examples: [
      { input: "nums = [1,1,1,2,2,3], k = 2", output: "[1,2]" },
    ],
    constraints: ["1 <= nums.length <= 10^5", "k is in range [1, number of unique elements]"],
    hints: ["Count frequencies with a hash map.", "Use a min-heap of size k or bucket sort by frequency."],
    approach: "Count frequencies, then use bucket sort (array indexed by frequency) or a min-heap of size k.",
    timeComplexity: "O(n)",
    spaceComplexity: "O(n)",
    tags: ["array", "hash-table", "heap"],
    starterCode: {
      javascript: "function topKFrequent(nums, k) {\n  // Your code here\n}",
      python: "def topKFrequent(nums, k):\n    pass",
      java: "class Solution {\n    public int[] topKFrequent(int[] nums, int k) {\n    }\n}",
    },
    solution: {
      javascript: "function topKFrequent(nums, k) {\n  const freq = new Map();\n  for (const n of nums) freq.set(n, (freq.get(n)||0)+1);\n  const buckets = Array.from({length: nums.length+1}, () => []);\n  for (const [num, count] of freq) buckets[count].push(num);\n  const res = [];\n  for (let i = buckets.length-1; i >= 0 && res.length < k; i--)\n    for (const n of buckets[i]) { res.push(n); if (res.length === k) return res; }\n  return res;\n}",
      python: "def topKFrequent(nums, k):\n    from collections import Counter\n    return [x for x, _ in Counter(nums).most_common(k)]",
      java: "class Solution {\n    public int[] topKFrequent(int[] nums, int k) {\n        Map<Integer,Integer> freq = new HashMap<>();\n        for (int n : nums) freq.merge(n, 1, Integer::sum);\n        List<int[]> list = new ArrayList<>();\n        for (var e : freq.entrySet()) list.add(new int[]{e.getValue(), e.getKey()});\n        list.sort((a,b) -> b[0]-a[0]);\n        int[] res = new int[k];\n        for (int i = 0; i < k; i++) res[i] = list.get(i)[1];\n        return res;\n    }\n}",
    },
  }),
];
