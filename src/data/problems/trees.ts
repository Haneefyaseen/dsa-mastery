import { p } from "./helpers";

export const treeProblems = [
  p({
    id: "invert-binary-tree",
    title: "Invert Binary Tree",
    difficulty: "easy",
    topicId: "trees",
    leetcodeId: 226,
    companies: ["Google", "Amazon", "Meta"],
    description: "Given the root of a binary tree, invert the tree, and return its root.",
    examples: [{ input: "root = [4,2,7,1,3,6,9]", output: "[4,7,2,9,6,3,1]" }],
    constraints: ["Number of nodes in range [0, 100]"],
    hints: ["Swap left and right children recursively.", "Can also use BFS level by level."],
    approach: "Recursively swap left and right subtrees at each node.",
    timeComplexity: "O(n)", spaceComplexity: "O(h)",
    tags: ["tree", "dfs", "bfs"],
    starterCode: { javascript: "function invertTree(root) {\n}", python: "def invertTree(root):\n    pass", java: "class Solution {\n    public TreeNode invertTree(TreeNode root) {\n    }\n}" },
    solution: {
      javascript: "function invertTree(root) {\n  if(!root) return null;\n  [root.left,root.right]=[invertTree(root.right),invertTree(root.left)];\n  return root;\n}",
      python: "def invertTree(root):\n    if not root: return None\n    root.left, root.right = invertTree(root.right), invertTree(root.left)\n    return root",
      java: "class Solution {\n    public TreeNode invertTree(TreeNode root) {\n        if(root==null) return null;\n        TreeNode tmp=root.left; root.left=invertTree(root.right); root.right=invertTree(tmp);\n        return root;\n    }\n}",
    },
  }),
  p({
    id: "max-depth",
    title: "Maximum Depth of Binary Tree",
    difficulty: "easy",
    topicId: "trees",
    leetcodeId: 104,
    companies: ["Amazon", "LinkedIn", "Microsoft"],
    description: "Given the root of a binary tree, return its maximum depth.",
    examples: [{ input: "root = [3,9,20,null,null,15,7]", output: "3" }],
    constraints: ["Number of nodes in [0, 10^4]"],
    hints: ["Depth = 1 + max(left depth, right depth).", "Base case: null node has depth 0."],
    approach: "Recursive DFS returning 1 + max of subtree depths.",
    timeComplexity: "O(n)", spaceComplexity: "O(h)",
    tags: ["tree", "dfs"],
    starterCode: { javascript: "function maxDepth(root) {\n}", python: "def maxDepth(root):\n    pass", java: "class Solution {\n    public int maxDepth(TreeNode root) {\n    }\n}" },
    solution: {
      javascript: "function maxDepth(root) {\n  if(!root) return 0;\n  return 1+Math.max(maxDepth(root.left),maxDepth(root.right));\n}",
      python: "def maxDepth(root):\n    return 0 if not root else 1+max(maxDepth(root.left),maxDepth(root.right))",
      java: "class Solution {\n    public int maxDepth(TreeNode root) {\n        return root==null?0:1+Math.max(maxDepth(root.left),maxDepth(root.right));\n    }\n}",
    },
  }),
  p({
    id: "same-tree",
    title: "Same Tree",
    difficulty: "easy",
    topicId: "trees",
    leetcodeId: 100,
    companies: ["Amazon", "Bloomberg"],
    description: "Given the roots of two binary trees `p` and `q`, check if they are the same.",
    examples: [{ input: "p = [1,2,3], q = [1,2,3]", output: "true" }],
    constraints: ["Number of nodes in [0, 100]"],
    hints: ["Compare values and recursively check both subtrees."],
    approach: "DFS comparing node values and both children.",
    timeComplexity: "O(n)", spaceComplexity: "O(h)",
    tags: ["tree", "dfs"],
    starterCode: { javascript: "function isSameTree(p, q) {\n}", python: "def isSameTree(p, q):\n    pass", java: "class Solution {\n    public boolean isSameTree(TreeNode p, TreeNode q) {\n    }\n}" },
    solution: {
      javascript: "function isSameTree(p, q) {\n  if(!p&&!q) return true;\n  if(!p||!q||p.val!==q.val) return false;\n  return isSameTree(p.left,q.left)&&isSameTree(p.right,q.right);\n}",
      python: "def isSameTree(p, q):\n    if not p and not q: return True\n    if not p or not q or p.val!=q.val: return False\n    return isSameTree(p.left,q.left) and isSameTree(p.right,q.right)",
      java: "class Solution {\n    public boolean isSameTree(TreeNode p, TreeNode q) {\n        if(p==null&&q==null) return true;\n        if(p==null||q==null||p.val!=q.val) return false;\n        return isSameTree(p.left,q.left)&&isSameTree(p.right,q.right);\n    }\n}",
    },
  }),
  p({
    id: "subtree-of-another",
    title: "Subtree of Another Tree",
    difficulty: "easy",
    topicId: "trees",
    leetcodeId: 572,
    companies: ["Meta", "Amazon"],
    description: "Given the roots of two binary trees `root` and `subRoot`, return true if `subRoot` is a subtree of `root`.",
    examples: [{ input: "root = [3,4,5,1,2], subRoot = [4,1,2]", output: "true" }],
    constraints: ["Number of nodes in [1, 2000]"],
    hints: ["At each node, check if trees match using same-tree logic."],
    approach: "DFS on root; at each node check if subtree starting there matches subRoot.",
    timeComplexity: "O(m*n)", spaceComplexity: "O(h)",
    tags: ["tree", "dfs"],
    starterCode: { javascript: "function isSubtree(root, subRoot) {\n}", python: "def isSubtree(root, subRoot):\n    pass", java: "class Solution {\n    public boolean isSubtree(TreeNode root, TreeNode subRoot) {\n    }\n}" },
    solution: {
      javascript: "function isSubtree(root, subRoot) {\n  if(!root) return false;\n  const same=(a,b)=>{if(!a&&!b)return true;if(!a||!b||a.val!==b.val)return false;return same(a.left,b.left)&&same(a.right,b.right);};\n  return same(root,subRoot)||isSubtree(root.left,subRoot)||isSubtree(root.right,subRoot);\n}",
      python: "def isSubtree(root, subRoot):\n    def same(a,b):\n        if not a and not b: return True\n        if not a or not b or a.val!=b.val: return False\n        return same(a.left,b.left) and same(a.right,b.right)\n    if not root: return False\n    return same(root,subRoot) or isSubtree(root.left,subRoot) or isSubtree(root.right,subRoot)",
      java: "class Solution {\n    public boolean isSubtree(TreeNode root, TreeNode subRoot) {\n        if(root==null) return false;\n        return same(root,subRoot)||isSubtree(root.left,subRoot)||isSubtree(root.right,subRoot);\n    }\n    boolean same(TreeNode a,TreeNode b){\n        if(a==null&&b==null) return true;\n        if(a==null||b==null||a.val!=b.val) return false;\n        return same(a.left,b.left)&&same(a.right,b.right);\n    }\n}",
    },
  }),
  p({
    id: "lowest-common-ancestor",
    title: "Lowest Common Ancestor of BST",
    difficulty: "medium",
    topicId: "trees",
    leetcodeId: 235,
    companies: ["Meta", "Amazon", "Microsoft"],
    description: "Given a BST, find the lowest common ancestor of two given nodes.",
    examples: [{ input: "root = [6,2,8,0,4,7,9,null,null,3,5], p = 2, q = 8", output: "6" }],
    constraints: ["All node values are unique", "p and q exist in the BST"],
    hints: ["If both nodes are on the right, go right.", "If split occurs, current node is LCA."],
    approach: "Use BST property: if both values smaller go left, both larger go right, else current is LCA.",
    timeComplexity: "O(h)", spaceComplexity: "O(1)",
    tags: ["tree", "bst"],
    starterCode: { javascript: "function lowestCommonAncestor(root, p, q) {\n}", python: "def lowestCommonAncestor(root, p, q):\n    pass", java: "class Solution {\n    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {\n    }\n}" },
    solution: {
      javascript: "function lowestCommonAncestor(root, p, q) {\n  while(root){\n    if(p.val<root.val&&q.val<root.val) root=root.left;\n    else if(p.val>root.val&&q.val>root.val) root=root.right;\n    else return root;\n  }\n}",
      python: "def lowestCommonAncestor(root, p, q):\n    while root:\n        if p.val<root.val and q.val<root.val: root=root.left\n        elif p.val>root.val and q.val>root.val: root=root.right\n        else: return root",
      java: "class Solution {\n    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {\n        while(root!=null){\n            if(p.val<root.val&&q.val<root.val) root=root.left;\n            else if(p.val>root.val&&q.val>root.val) root=root.right;\n            else return root;\n        }\n        return null;\n    }\n}",
    },
  }),
  p({
    id: "validate-bst",
    title: "Validate Binary Search Tree",
    difficulty: "medium",
    topicId: "trees",
    leetcodeId: 98,
    companies: ["Amazon", "Meta", "Microsoft", "Bloomberg"],
    description: "Given the root of a binary tree, determine if it is a valid BST.",
    examples: [{ input: "root = [2,1,3]", output: "true" }],
    constraints: ["Number of nodes in [1, 10^4]"],
    hints: ["Each node must be within (min, max) bounds.", "Pass down valid range to children."],
    approach: "DFS with min/max bounds. Left child must be < parent, right must be > parent.",
    timeComplexity: "O(n)", spaceComplexity: "O(h)",
    tags: ["tree", "dfs", "bst"],
    starterCode: { javascript: "function isValidBST(root) {\n}", python: "def isValidBST(root):\n    pass", java: "class Solution {\n    public boolean isValidBST(TreeNode root) {\n    }\n}" },
    solution: {
      javascript: "function isValidBST(root) {\n  const dfs=(node,min,max)=>{\n    if(!node) return true;\n    if(node.val<=min||node.val>=max) return false;\n    return dfs(node.left,min,node.val)&&dfs(node.right,node.val,max);\n  };\n  return dfs(root,-Infinity,Infinity);\n}",
      python: "def isValidBST(root):\n    def dfs(node, lo, hi):\n        if not node: return True\n        if not lo < node.val < hi: return False\n        return dfs(node.left, lo, node.val) and dfs(node.right, node.val, hi)\n    return dfs(root, float('-inf'), float('inf'))",
      java: "class Solution {\n    public boolean isValidBST(TreeNode root) {\n        return dfs(root,null,null);\n    }\n    boolean dfs(TreeNode n,Integer min,Integer max){\n        if(n==null) return true;\n        if(min!=null&&n.val<=min||max!=null&&n.val>=max) return false;\n        return dfs(n.left,n.val,max)&&dfs(n.right,min,n.val);\n    }\n}",
    },
  }),
  p({
    id: "kth-smallest-bst",
    title: "Kth Smallest Element in a BST",
    difficulty: "medium",
    topicId: "trees",
    leetcodeId: 230,
    companies: ["Amazon", "Meta", "Google"],
    description: "Given the root of a BST and an integer `k`, return the kth smallest value.",
    examples: [{ input: "root = [3,1,4,null,2], k = 1", output: "1" }],
    constraints: ["1 <= k <= number of nodes"],
    hints: ["Inorder traversal gives sorted order.", "Stop when count reaches k."],
    approach: "Iterative inorder traversal, return kth visited node.",
    timeComplexity: "O(h+k)", spaceComplexity: "O(h)",
    tags: ["tree", "bst", "dfs"],
    starterCode: { javascript: "function kthSmallest(root, k) {\n}", python: "def kthSmallest(root, k):\n    pass", java: "class Solution {\n    public int kthSmallest(TreeNode root, int k) {\n    }\n}" },
    solution: {
      javascript: "function kthSmallest(root, k) {\n  const stack=[]; let cur=root;\n  while(cur||stack.length){\n    while(cur){stack.push(cur);cur=cur.left;}\n    cur=stack.pop(); if(--k===0) return cur.val;\n    cur=cur.right;\n  }\n}",
      python: "def kthSmallest(root, k):\n    stack=[]; cur=root\n    while cur or stack:\n        while cur: stack.append(cur); cur=cur.left\n        cur=stack.pop(); k-=1\n        if k==0: return cur.val\n        cur=cur.right",
      java: "class Solution {\n    public int kthSmallest(TreeNode root, int k) {\n        Deque<TreeNode> st=new ArrayDeque<>(); TreeNode cur=root;\n        while(cur!=null||!st.isEmpty()){\n            while(cur!=null){st.push(cur);cur=cur.left;}\n            cur=st.pop(); if(--k==0) return cur.val; cur=cur.right;\n        }\n        return -1;\n    }\n}",
    },
  }),
  p({
    id: "serialize-deserialize",
    title: "Serialize and Deserialize Binary Tree",
    difficulty: "hard",
    topicId: "trees",
    leetcodeId: 297,
    companies: ["Meta", "Amazon", "Google", "Microsoft"],
    description: "Design an algorithm to serialize and deserialize a binary tree.",
    examples: [{ input: "root = [1,2,3,null,null,4,5]", output: "[1,2,3,null,null,4,5]" }],
    constraints: ["Number of nodes in [0, 10^4]"],
    hints: ["Preorder traversal with null markers works well.", "Use BFS with null markers as alternative."],
    approach: "Preorder DFS serialize with 'N' for null. Deserialize by reading values in order.",
    timeComplexity: "O(n)", spaceComplexity: "O(n)",
    tags: ["tree", "dfs", "design"],
    starterCode: { javascript: "function serialize(root) {\n}\nfunction deserialize(data) {\n}", python: "def serialize(root):\n    pass\ndef deserialize(data):\n    pass", java: "class Codec {\n    public String serialize(TreeNode root) {\n    }\n    public TreeNode deserialize(String data) {\n    }\n}" },
    solution: {
      javascript: "function serialize(root) {\n  const res=[];\n  const dfs=(n)=>{if(!n){res.push('N');return;}res.push(String(n.val));dfs(n.left);dfs(n.right);};\n  dfs(root); return res.join(',');\n}\nfunction deserialize(data) {\n  const vals=data.split(','); let i=0;\n  const dfs=()=>{if(vals[i]==='N'){i++;return null;}const n={val:+vals[i++],left:null,right:null};n.left=dfs();n.right=dfs();return n;};\n  return dfs();\n}",
      python: "def serialize(root):\n    def dfs(n):\n        if not n: return ['N']\n        return [str(n.val)] + dfs(n.left) + dfs(n.right)\n    return ','.join(dfs(root))\ndef deserialize(data):\n    vals=iter(data.split(','))\n    def dfs():\n        v=next(vals)\n        if v=='N': return None\n        node=TreeNode(int(v)); node.left=dfs(); node.right=dfs(); return node\n    return dfs()",
      java: "class Codec {\n    public String serialize(TreeNode root) {\n        StringBuilder sb=new StringBuilder(); dfsSer(root,sb); return sb.toString();\n    }\n    void dfsSer(TreeNode n,StringBuilder sb){\n        if(n==null){sb.append(\"N,\");return;}\n        sb.append(n.val).append(','); dfsSer(n.left,sb); dfsSer(n.right,sb);\n    }\n    public TreeNode deserialize(String data) {\n        Queue<String> q=new LinkedList<>(Arrays.asList(data.split(\",\")));\n        return dfsDes(q);\n    }\n    TreeNode dfsDes(Queue<String> q){\n        String v=q.poll(); if(v.equals(\"N\")) return null;\n        TreeNode n=new TreeNode(Integer.parseInt(v)); n.left=dfsDes(q); n.right=dfsDes(q); return n;\n    }\n}",
    },
  }),
];
